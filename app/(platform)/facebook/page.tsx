import { JSX } from "react";
import { PagesList } from "./_components";

const Index = (): JSX.Element => {
  return (
    <div className={`mt-5`}>
      <PagesList />
    </div>
  );
};

export default Index;
